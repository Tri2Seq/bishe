# 多楼层多机器人协同配送与计算资源调度

## 一句话

20台机器人、4部电梯、15层楼——如何在一天内完成 300+ 次跨楼层配送，同时让云端GPU不空转也不过载？

---

## 问题全景

```
                    ┌──────────────────────────────┐
                    │     云端 GPU 集群 (模块2)      │
                    │  深度估计推理 · 碎片整理 · 预测  │
                    └──────────┬───────────────────┘
                               │ 按电梯时需要GPU
                               │ 乘电梯时可释放
              ┌────────────────┼────────────────┐
              │                │                │
         ┌────▼────┐     ┌────▼────┐     ┌────▼────┐
         │ 机器人 1 │     │ 机器人 2 │     │   ...   │
         └────┬────┘     └────┬────┘     └────┬────┘
              │  取货-乘电梯-送货  │               │
              ▼                ▼               ▼
    ┌─────────────────────────────────────────────────┐
    │          电梯群控系统 (ETA/SCAN/NC)                │
    │  机器人按按钮 → 群控系统决定派哪部电梯 → 机器人等待搭乘  │
    └─────────────────────────────────────────────────┘
              │                │               │
        ┌─────▼──┐       ┌────▼───┐      ┌────▼───┐
        │ 电梯 1  │       │ 电梯 2  │      │ 电梯 3  │  ...
        └────────┘       └────────┘      └────────┘
```

**两个模块串联，不做联合优化：**

- **模块1** 只管配送——任务分配、执行顺序、机器人策略
- **模块2** 只管GPU——根据模块1产生的动作时间线调度推理资源

---

## 模块1：配送调度

### 输入

| 要素 | 说明 |
|------|------|
| 楼宇 | F 层 (5–20)，各楼层有取货站/送货站 |
| 机器人 | M 台 (5–40)，重载/标准/快递三型，载重 10–80kg |
| 电梯 | E 部 (2–6)，货梯/客梯，容量 1–2 |
| 任务 | N 个 (10–300)，各有起点楼层、终点楼层、重量、优先级、截止时间 |

### 输出

每台机器人的完整动作时间线：

```
走路 → [接近电梯★GPU] → [按按钮★GPU] → 等电梯 → 乘电梯 → [出电梯★GPU] → 走路 → 送货
```

带 ★ 的阶段需要云端深度估计推理（GPU资源），其余不需要。

### 电梯群控系统

机器人**不选择**电梯。按下按钮后，群控系统根据派车算法决定派哪部：

| 算法 | 逻辑 | 特点 |
|------|------|------|
| **ETA** | 选预估到达时间最短的 | 最快到达 |
| **SCAN** | 电梯沿一个方向扫描到头再折返 | 公平性好 |
| **NC** | 选最近的空闲电梯 | 简单直接 |

### 机器人策略

在群控系统下，机器人能做的决策：

| 策略 | 做什么 | 效果 |
|------|--------|------|
| 批量携带 | 同楼层多个包裹一起取，一次乘电梯 | 减少电梯使用次数 |
| 多站送货 | 携带多包裹，沿途逐层送达 | 减少往返 |
| 错峰呼叫 | 同楼层已有人等电梯时延迟呼叫 | 降低拥挤 |

### 求解算法

```
    精确求解（小规模 ≤20 任务）          启发式（大规模 50–300 任务）
  ┌───────────────────────┐      ┌─────────────────────────────┐
  │  MILP V2              │      │  NSGA-II-EBO (本文核心算法)    │
  │  · 电梯 disjunctive    │      │  · NSGA-II 框架保持 Pareto 多样│
  │    约束(不相交)         │      │  · 下层优化器利用电梯瓶颈结构   │
  │  · 重定位时间           │      │  · 冲突感知交叉 + 关键任务优先  │
  │  · 12 条约束           │      │  · Makespan 比标准 NSGA-II    │
  │  · 3 目标加权和         │      │    低 10.3%                  │
  └───────────────────────┘      ├─────────────────────────────┤
                                  │  对比算法                     │
                                  │  · NSGA-II (标准)             │
                                  │  · ALNS (自适应大邻域搜索)     │
                                  │  · Greedy-FCFS (贪心基线)     │
                                  └─────────────────────────────┘
```

### 关键结果

**50 任务、15 机器人、4 电梯、15 层：**

| 方法 | Makespan | 能耗 | 延迟 | 时间 |
|------|----------|------|------|------|
| Greedy | 617s | 17940J | 854 | <0.01s |
| NSGA-II | 490s | 15900J | 0 | 29s |
| **NSGA-II-EBO** | **439s** | **15690J** | **0** | 34s |

NSGA-II-EBO 比标准 NSGA-II Makespan **降低 10.3%**，因为下层优化器利用了电梯瓶颈结构。

---

## 模块2：GPU 资源调度

### 问题

机器人按电梯按钮时需要深度估计推理（占 4–6GB 显存），乘电梯时不需要。GPU 有限，多台机器人同时需要时怎么办？

### 调度策略

| 策略 | 思路 | 冷启动 | 等待延迟 |
|------|------|--------|---------|
| **Predictive** | 预测未来需求，提前加载模型 | 80 | **0s** |
| FragAware | 碎片感知 + 保持/释放决策 | 214 | 0s |
| OnDemand | 要了就分，用完就释放 | 73 | **675s** |
| RoundRobin | 轮询 | 73 | 675s |

**Predictive 的优势**：冷启动比 FragAware **少 63%**（80 vs 214），等待延迟和 FragAware 一样为零。

### 24 小时周期

任务量随时段变化，GPU 需求跟着波动：

```
任务量
  40│          ██
  30│     ████ ████
  20│     ████ ████
  10│ ██  ████ ████  ████
   0│██████████████████████████
    6  8  10  12  14  16  18  20  22  时刻

    ←低→ ←高峰→ ←低→ ←高峰→ ←低→
           ↑ GPU 预加载     ↑ GPU 预释放
```

Predictive 调度器在高峰到来前预加载模型，在低谷到来前预释放——基于已知的每日周期模式。

---

## 项目结构

```
gra/
├── RESEARCH_BRIEF.md          # 完整问题定义
├── IDEA_REPORT.md             # 实验结果报告
├── AUTO_REVIEW.md             # 审查改进记录
│
├── module1/                   # 配送调度
│   ├── config.py              # 数据结构 · 场景生成
│   ├── elevator_group_control.py  # 电梯群控系统 (ETA/SCAN/NC)
│   ├── simulator_v2.py        # 仿真器 (群控 + 批量 + 策略)
│   ├── dynamic_scenario.py    # 24h 周期性任务生成
│   ├── problem_analysis.py    # 电梯瓶颈下界 · 冲突图 · 关键任务
│   │
│   ├── milp_v2.py             # MILP 精确求解 (disjunctive)
│   ├── nsga2.py               # NSGA-II 多目标进化
│   ├── nsga2_ebo.py           # NSGA-II-EBO (核心算法)
│   ├── alns.py                # ALNS 自适应大邻域搜索
│   ├── ebdco.py               # EBDCO 分解协同优化
│   ├── baselines.py           # Greedy-FCFS · 独立规划
│   │
│   ├── visualize.py           # Gantt · 电梯时空 · Pareto · 收敛
│   ├── full_experiment.py     # 统计实验 (多规模 × 多方法 × 多seed)
│   └── results/               # 实验数据 + 图表
│
├── module2/                   # GPU 调度
│   ├── gpu_config.py          # GPU 集群配置
│   ├── gpu_scheduler.py       # 5 种调度策略
│   ├── gpu_milp.py            # GPU 分配 MILP
│   └── full_gpu_experiment.py # GPU 灵敏度实验
│
└── 硕士研究生选题报告表-sjx.pdf  # 选题报告
```

**代码量**：27 个 Python 文件，约 8300 行。

---

## 创新点

| # | 创新 | 对比 |
|---|------|------|
| 1 | 电梯群控系统下的机器人策略优化 | 现有工作中机器人直接选电梯 |
| 2 | NSGA-II-EBO：下层利用电梯瓶颈结构 | 标准 NSGA-II 黑箱搜索 |
| 3 | 机器人动作驱动的 GPU 预测调度 | 现有 GPU 调度不感知机器人状态 |
| 4 | 24 小时周期建模 + 峰谷预加载/预释放 | 现有工作仅做静态批处理 |
| 5 | 批量携带 + 多站送货 | 现有工作一次只送一个包裹 |

---

## 快速上手

```bash
cd module1

# 小规模 MILP 精确求解
python test_milp_v2.py

# NSGA-II-EBO vs NSGA-II 对比
python test_ebdco.py

# 50 任务仿真 (电梯群控)
python -c "
from dynamic_scenario import *
from simulator_v2 import *
from baselines import greedy_fcfs
s = DayScenario(num_floors=15, num_robots=20, num_elevators=4,
                num_gpu_servers=2, gpus_per_server=4)
tasks = generate_day_tasks(s)[:50]
robots = generate_day_robots(s)
elevs = generate_day_elevators(s)
for i,t in enumerate(tasks): t.id=i; t.earliest_start=0
g = greedy_fcfs(robots, elevs, tasks)
sim = SimulatorV2(robots, elevs, tasks)
sim.load_schedule(g['assignment'], g['task_sequence'])
r = sim.run(max_time=5000)
sim.print_summary(r)
"

# GPU 调度对比
cd ../module2
python run_gpu_experiment.py medium tight
```

---

## 待完成

- [ ] Phase 5：NSGA-II-EBO 全面迁移到 V2 仿真器
- [ ] Phase 6：24 小时全链路实验 (配送 + GPU 联合)
- [ ] 论文撰写 (目标：T-ASE / RA-L / IROS)
