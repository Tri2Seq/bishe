# 研究简报：面向多楼层场景的多机器人协同配送与计算资源调度

## 整体架构：两模块串联

```
┌─────────────────────────────────────────┐
│  模块1：多机器人跨楼层协同配送调度         │
│  输入：任务集、机器人车队、电梯、楼层布局    │
│  输出：每台机器人的完整动作时间线           │
│       (行走→等电梯→按按钮→乘梯→出梯→行走) │
└──────────────────┬──────────────────────┘
                   │ 动作时间线
                   ↓
┌─────────────────────────────────────────┐
│  模块2：基于机器人动作的GPU资源调度         │
│  输入：所有机器人的动作时间线              │
│  输出：GPU分配方案、迁移策略               │
│       (谁的推理任务在哪张GPU上、何时迁移)   │
└─────────────────────────────────────────┘
```

模块1不感知GPU，只关心物理世界的优化。
模块2不改变机器人调度，只根据已知的动作序列优化计算资源。

---

# 模块1：多机器人跨楼层协同配送调度

## 1.1 场景描述

### 物理环境

一座**大型智能物流园区/工厂楼宇**，共 `F` 层（典型值 F=10~20），配备 `E` 部电梯（典型值 E=4~6）。

**具体场景**：某大型电商区域分拣中心（20层）
- **1-2层**：收货入库区——外部货物到达，扫码分类
- **3-8层**：小件存储区——按品类分区（电子、日用、食品等）
- **9-14层**：中大件存储区——家具、家电、运动器材等
- **15-17层**：打包质检区——订单合并、质量检查
- **18-20层**：发货分拣区——按快递公司/区域分拣装车

货物流向：上行（收货→存储）、下行（存储→打包→发货）、横向调拨，形成大规模双向交叉流量。

### 机器人车队

`M` 台异构机器人（典型值 M=15~30），三类：

| 类型 | 占比 | 载重(kg) | 速度(m/s) | 续航(m) | 特点 |
|------|------|---------|----------|--------|------|
| 重载型 | ~30% | 80 | 0.8 | 3000 | 大件搬运 |
| 标准型 | ~50% | 30 | 1.5 | 2500 | 通用 |
| 快递型 | ~20% | 10 | 2.5 | 2000 | 轻件快送 |

### 电梯系统

`E` 部电梯（典型值 E=4~6），差异化配置：

| 类型 | 数量 | 机器人容量/趟 | 速度(层/s) | 开关门(s) |
|------|------|------------|----------|----------|
| 货梯 | 2 | 2 | 0.4 | 8 |
| 客梯 | 2~4 | 1 | 0.6 | 5 |

### 配送任务

一批 `N` 个配送任务（典型值 N=50~200）：
```
Task_i = (o_i, dest_i, W_i, p_i, l_i)
```
- `o_i`：取货楼层+站点
- `dest_i`：送货楼层+站点
- `W_i`：货物重量(kg)
- `p_i`：优先级 ∈ {1=紧急, 2=普通, 3=批量}
- `l_i`：交付截止时间(s)

## 1.2 决策变量

**任务分配**：
- `x_{ij} ∈ {0,1}`：机器人 `j` 执行任务 `i`

**任务排序**：
- `y_{ii'j} ∈ {0,1}`：机器人 `j` 在任务 `i` 之后紧接执行任务 `i'`

**电梯选择**：
- `z_{ike} ∈ {0,1}`：任务 `i` 的第 `k` 次跨层行程使用电梯 `e`

**时序变量**：
- `s_i`：任务 `i` 取货开始时间
- `c_i`：任务 `i` 送达完成时间
- `a_{ike}`：机器人到达电梯 `e` 等候区时间
- `b_{ike}`：机器人实际进入电梯时间

## 1.3 目标函数

### 目标1：最小化总完工时间
```
F₁ = max_{i} (c_i)
```

### 目标2：最小化总能耗
```
F₂ = Σ_j Σ_i x_{ij}·D_i·μ_j + Σ_e λ_e·Σ_{trips}|Δfloors|
       (机器人行驶能耗)          (电梯运行能耗)
```

### 目标3：最小化加权延迟
```
F₃ = Σ_i w(p_i)·max(0, c_i - l_i)
```

### 多目标求解
ε-约束法或 NSGA-II 求Pareto前沿。

## 1.4 约束条件

**C1 任务唯一性**：
```
Σ_j x_{ij} = 1,  ∀i
```

**C2 载重约束**：
```
Σ_{i∈carry(j,t)} W_i·x_{ij} ≤ W_j^max,  ∀j, ∀t
```

**C3 续航约束**：
```
Σ_i x_{ij}·D_i^total ≤ D_j^max,  ∀j
```

**C4 电梯容量约束**：
```
|{j : robot j in elevator e at time t}| ≤ Q_e,  ∀e, ∀t
```

**C5 电梯不可抢占**：
```
电梯e在运行区间 [t_depart, t_arrive] 内不可被登入
```

**C6 时间窗约束**：
```
s_i ≥ e_i,  ∀i
```

**C7 排序时序一致性**：
```
s_{i'} ≥ c_i + travel(dropoff_i → pickup_{i'}) + wait_elevator,  若 y_{ii'j}=1
```

## 1.5 模块1的输出

模块1求解后，为每台机器人生成一条**动作时间线**：

```
机器人j的动作序列 A_j = [(action, start_time, end_time, floor, details), ...]

动作类型:
  WALK(f, s1, s2)        — 在楼层f内从s1走到s2
  WAIT_ELEVATOR(f, e)    — 在楼层f等待电梯e
  APPROACH_ELEVATOR(f,e) — 接近电梯e，准备按按钮     ← ★需要深度估计★
  PRESS_BUTTON(f, e)     — 按下电梯按钮             ← ★需要深度估计★  
  RIDE_ELEVATOR(e, f1→f2)— 乘坐电梯从f1到f2         ← 不需要深度估计
  EXIT_ELEVATOR(f, e)    — 出电梯，环境感知          ← ★需要深度估计★
  PICKUP(f, station)     — 在站点取货
  DELIVER(f, station)    — 在站点送货
  IDLE                   — 空闲等待
```

**核心**：这条时间线明确标注了每台机器人在**什么时刻需要深度估计推理、什么时刻不需要**，成为模块2的输入。

---

# 模块2：基于机器人动作的GPU资源调度

## 2.1 问题定义

### 输入
来自模块1的 `M` 条机器人动作时间线。从中提取每台机器人的**深度估计需求时段**：

```
机器人j的GPU需求序列: G_j = [(t_start, t_end, type), ...]

其中type:
  APPROACH — 接近电梯，需要实时深度估计（持续~15s）
  PRESS    — 按按钮，需要高精度深度估计（持续~10s）
  EXIT     — 出电梯导航（持续~5s）
```

同时可以提取**GPU空闲窗口**（机器人不需要深度估计的时段）：
```
机器人j的空闲窗口: I_j = [(t_start, t_end, reason), ...]

reason:
  WALKING    — 楼层内行走，无需深度估计
  RIDING     — 乘坐电梯中
  LOADING    — 装卸货物中
  IDLE       — 空闲
```

### 云端GPU集群

`P` 台服务器，每台 `G` 张GPU：

| 参数 | 值 | 说明 |
|------|---|------|
| GPU总数 | P×G（典型8~32张） | |
| 单GPU显存 V_max | 24 GB | |
| 深度估计模型占用 v_depth | 4 GB | 单个推理实例 |
| 单GPU最大并发 | ⌊V_max/v_depth⌋ = 6 | |
| 推理延迟 | ~50ms/帧 | |
| 冷启动（加载模型） t_cold | ~3s | |
| 热迁移 t_migrate | ~1s | GPU间迁移 |

### 核心问题

给定所有机器人的GPU需求时间表，如何将这些推理任务分配到有限的GPU上，使得：
- 每个需求时段都有GPU可用（不产生等待）
- GPU显存碎片率最低
- 迁移次数/开销最小

## 2.2 决策变量

**GPU分配**：
- `h_{jkg} ∈ {0,1}`：机器人 `j` 的第 `k` 个需求时段分配到GPU `g`

**迁移决策**：
- `m_{jkg₁g₂} ∈ {0,1}`：在需求段间隙，将机器人 `j` 的模型从GPU `g₁` 迁移到GPU `g₂`

**预加载决策**：
- `l_{jkg} ∈ {0,1}`：在机器人 `j` 的第 `k` 个需求时段开始前，提前在GPU `g` 上加载模型

**释放决策**：
- `r_{jkg} ∈ {0,1}`：在机器人 `j` 的第 `k` 个需求时段结束后，释放GPU `g` 上的模型

## 2.3 目标函数

### 目标A：最小化集群总显存碎片率

基于选题报告中的碎片率定义。设GPU `g` 在时刻 `t` 的剩余显存为 `r_{g,t}`，碎片率函数：
```
φ(r_{g,t}) = Σ_{s∈S: s > r_{g,t}} π_s
```
其中 `S` 为历史任务显存需求集合，`π_s` 为需求分布概率。

集群总碎片率目标：
```
F_A = (1/T)·∫₀ᵀ Σ_g φ(r_{g,t}) dt
      即：时间平均的集群总碎片率
```

### 目标B：最小化总迁移开销
```
F_B = Σ_{j,k} Σ_{g₁,g₂} m_{jkg₁g₂}·t_migrate
```

### 目标C：最小化GPU等待延迟（如果GPU不够导致某些需求被延迟）
```
F_C = Σ_{j,k} max(0, gpu_ready_{jk} - demand_start_{jk})
```
即GPU实际就绪时间与需求开始时间的差。理想情况下 F_C=0。

### 多目标处理
```
min  β₁·F̃_A + β₂·F̃_B + β₃·F̃_C
```

## 2.4 约束条件

**C8 显存容量**：任意时刻，GPU上活跃任务的总显存不超过上限
```
Σ_{(j,k): active(j,k,t)} v_depth · h_{jkg} ≤ V_max,  ∀g, ∀t
```

**C9 需求必须满足**：每个GPU需求时段必须有且仅有一张GPU分配
```
Σ_g h_{jkg} = 1,  ∀j, ∀k
```

**C10 冷启动约束**：若机器人 `j` 的模型不在任何GPU上，重新加载需要 `t_cold`
```
若 r_{j,k-1,g}=1（上一段结束后释放了），则:
  模型加载开始时间 ≤ demand_start_{jk} - t_cold
```

**C11 迁移约束**：迁移期间源GPU和目标GPU都需要占用显存
```
迁移 j: g₁→g₂ 在 [t, t+t_migrate] 内:
  g₁ 上占用 v_depth（未完全释放）
  g₂ 上占用 v_depth（已预留）
```

**C12 迁移窗口约束**：迁移只能在机器人的GPU空闲窗口内进行
```
迁移操作必须在 I_j 的某个空闲窗口 [t_s, t_e] 内完成:
  t ≥ t_s 且 t + t_migrate ≤ t_e
```

**C13 预加载约束**：预加载必须在需求开始前完成
```
若 l_{jkg}=1:
  preload_start + t_cold ≤ demand_start_{jk}
  且 preload期间 GPU g 有足够空闲显存
```

## 2.5 GPU调度策略的关键决策

给定机器人动作时间线，GPU调度器面临三类决策：

### 决策1：保持 vs 释放

机器人进入乘梯阶段（RIDE_ELEVATOR），是否释放其GPU显存？

```
短途（跨1~2层，乘梯~10s）：
  释放后重新加载需要 t_cold=3s → 净省时间仅7s
  决策：保持（不值得释放）

长途（跨5+层，乘梯~25s+）：
  释放后重新加载需要 t_cold=3s → 净省时间>22s
  决策：释放（腾出空间给其他机器人）
```

### 决策2：迁移 vs 不迁移

当多个推理任务集中在少数GPU上导致碎片化，是否在空闲窗口迁移部分任务到其他GPU？

```
迁移收益：降低碎片率，为后续大任务腾出连续显存
迁移代价：t_migrate=1s的延迟 + 迁移期间双份显存占用
决策依据：碎片率函数 φ(r_g) 的变化量是否足以补偿迁移代价
```

### 决策3：预加载时机

为即将需要深度估计的机器人提前加载模型，何时开始？

```
太早：占用GPU显存，影响其他活跃任务
太晚：冷启动延迟导致机器人在电梯前等待
最佳：在需求开始前恰好 t_cold 时间开始加载
```

## 2.6 机器人动作驱动的GPU需求模式

```
时间 →
机器人1: ──走路──[★GPU★接近+按按钮]──乘梯──[★GPU★出梯]──走路──...
机器人2: ──走路──────[★GPU★接近+按按钮]──乘梯──[★GPU★出梯]──...
机器人3: ──乘梯──[★GPU★出梯]──走路──────[★GPU★接近+按按钮]──乘梯──...

GPU需求时序（从动作时间线中提取）:
t=0    t=50   t=65  t=80   t=110  t=125 t=140  t=170 t=185
 │      │      │     │      │      │     │      │     │
 R1需求──┤释放──┤R1需求┤                               
        R2需求────────┤释放──────┤R2需求┤                
 R3需求──┤            R3需求────────────┤释放──┤R3需求──┤

GPU分配示例（2张GPU，每张最多6并发）:
GPU#1: [R1][  ][R1][R2][    ][R2][R3][ ][ ]
GPU#2: [R3][  ][  ][R3][    ][  ][  ][ ][ ]
        ↑               ↑
     R3乘梯,释放     R3走路后又需要GPU
```

---

## 问题规模

| 规模 | 楼层F | 机器人M | 电梯E | 任务N | GPU服务器 | GPU总数 |
|------|------|--------|------|------|---------|--------|
| 小型 | 5 | 5 | 2 | 20 | 1×4 | 4 |
| 中型 | 10 | 15 | 4 | 80 | 2×4 | 8 |
| 大型 | 15 | 25 | 4 | 150 | 2×8 | 16 |
| 超大型 | 20 | 40 | 6 | 300 | 4×8 | 32 |

GPU紧张程度取决于 **同时需要深度估计的机器人数 vs GPU并发上限**。
以中型为例：15台机器人，8张GPU×6并发=48并发上限，通常不紧张。
但在电梯高峰时段，可能有10+台机器人同时到达电梯 → 接近上限。

---

## 求解方法

### 模块1
1. **MILP精确求解**（小型实例N≤20，验证最优性）
2. **分层启发式**：上层任务分配（拍卖/遗传算法）→ 下层电梯调度（优先队列/DP）
3. **NSGA-II/ALNS** 求Pareto前沿（中大型实例）

### 模块2
1. **MILP精确求解**（给定动作时间线，求最优GPU分配 + 迁移策略）
2. **在线贪心策略**：
   - 需求到来时：分配碎片率最低的GPU
   - 空闲窗口开始时：评估释放/迁移收益，超过阈值则执行
   - 需求即将到来时：提前 `t_cold` 开始预加载
3. **滚动优化**：每隔 T 秒，根据未来窗口内的已知需求重新求解一次小规模MILP

---

## 实验设计

### 对比基线

**模块1基线**：
| 基线 | 描述 |
|------|------|
| B1: Greedy-FCFS | 贪心分配+先到先服务电梯 |
| B2: 单目标MILP | 仅优化makespan |
| B3: 无电梯感知VRP | 标准VRP，电梯后分配 |
| B4: 各机器人独立规划 | 无协同 |

**模块2基线**：
| 基线 | 描述 |
|------|------|
| B5: 按需分配-不迁移 | 需要时分配，用完释放，不迁移 |
| B6: 固定分配 | 每台机器人绑定一张GPU，不释放不迁移 |
| B7: Round-Robin | 轮询分配到各GPU |
| B8: InSS(Han2025) | 时空共享GPU调度 |

### 评价指标

**模块1指标**：Makespan、总能耗、加权延迟、电梯利用率、Pareto超体积

**模块2指标**：
- GPU显存碎片率（时间平均）
- GPU利用率
- GPU等待延迟（需求到来但GPU未就绪的时间）
- 迁移次数与迁移开销
- 冷启动次数

### 联合验证
模块1输出调度方案 → 模块2计算GPU指标 → 验证端到端可行性：
- 所有机器人在需要深度估计时是否都能及时获得GPU？
- GPU瓶颈是否导致实际执行偏离计划？

---

## 发表目标

**模块1方向**：
- IROS / ICRA（机器人顶会）
- IEEE T-ASE / TII（自动化/工业信息学期刊）

**模块2方向**：
- 已有 IFAC 2026 基础，可扩展为期刊版
- IEEE TPDS（并行与分布式系统）
- IEEE TCC（云计算）

**核心创新点**：
1. 面向多楼层场景的多机器人-多电梯-多任务协同调度MILP模型
2. 基于机器人动作时间线的GPU推理任务调度——利用乘梯窗口进行显存迁移与碎片整理
3. 两模块串联验证：物理层调度决策如何影响计算层资源需求模式
